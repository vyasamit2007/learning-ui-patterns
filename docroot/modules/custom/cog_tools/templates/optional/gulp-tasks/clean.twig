/**
 * @file
 * Task: Clean.
 */

 /* global module */

module.exports = function (gulp, plugins, options) {
  'use strict';

  gulp.task('clean', [
    'clean:css',
{% if style_guide == 1 %}
    'clean:styleguide'
{% endif %}
  ]);
};
